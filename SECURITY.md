# RadishLink 安全策略

RadishLink 涉及设备身份、端到端加密、无线中继、升级和用户通信数据。安全问题应优先私下处理，避免公开报告在修复前扩大风险。

## 私下报告

如果远程仓库已经启用 GitHub Private Vulnerability Reporting，请使用仓库 `Security` 页面中的私密报告入口。若该入口不可用，请先通过仓库所有者的 GitHub 资料页请求建立私密联系渠道，不要在首次公开消息中附带漏洞细节。

报告尽量包含：

- 受影响的提交、组件、设备角色和部署条件；
- 可复现步骤、预期行为和实际行为；
- 对机密性、完整性、可用性、隐私或无线电合规的影响；
- 已做脱敏的日志、数据包或最小测试样例；
- 已知缓解措施和建议披露时间。

不得发送真实私钥、恢复码、设备凭据、私密通信内容或未经授权取得的第三方数据。若复现需要敏感材料，先描述材料类型并等待安全传输安排。

## 安全问题范围

- 身份冒用、未授权配对、密钥泄露、降级或重放；
- 消息、附件或媒体端到端加密失效；
- 中继越权解密、过量元数据暴露或用户数据泄露；
- Secure Boot、固件签名、回滚保护、OTA 或恢复链路绕过；
- 路由、存储转发、资源耗尽和恶意输入造成的安全影响；
- 手机伴侣接入、管理界面、本地存储和调试接口越权；
- 构建、依赖、CI、发布或设备供应链完整性问题；
- 监管域、功率、带宽或天线限制可被非授权绕过。

普通功能缺陷、文档错误和不含安全影响的性能问题使用常规 Issue 流程。

## 处理与披露

项目尚未承诺固定响应 SLA、受支持版本范围或公开发布周期。维护者会先确认报告接收、评估影响和复现条件，再协调修复与披露。未经协调，不应在修复可用前公开利用细节。

安全修复通过受保护分支的 PR 流程进入 `master`；必要时可使用私有修复分支或 GitHub Security Advisory 临时私有分叉。修复合并后仍必须回流 `dev`，且不得把漏洞样本中的真实秘密或用户内容带入长期 fixture。
