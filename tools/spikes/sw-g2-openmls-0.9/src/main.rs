#![forbid(unsafe_code)]

fn main() {
    eprintln!(
        "SW-EXP-004 Phase B is not implemented; only the reviewed Phase A runner may prepare the dependency graph"
    );
    std::process::exit(2);
}
