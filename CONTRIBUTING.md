# 参与 RadishLink

感谢对 RadishLink 的关注。本项目当前处于定义、法规预检和三节点 POC 准备阶段；贡献的首要目标是缩小不确定性、建立可复核证据，而不是提前扩大产品承诺。

## 贡献前先确认

- 安全漏洞不要提交公开 Issue 或 Pull Request，按[安全策略](SECURITY.md)私下报告。
- 射频法规、密码协议、设备身份、密钥、升级、公共协议或用户数据相关变更，应先通过 Issue 或设计讨论确认边界。
- 当前尚未冻结开源许可证和外部贡献授权机制。外部提交者不得推定仓库内容已经获得开放复用授权；在许可与贡献条款确定前，维护者可以暂缓合并外部代码或大段文档。
- 不提交真实密钥、恢复码、设备凭据、私密通信、未经脱敏的日志或无法确认授权的第三方材料。
- 不提交解锁监管域、绕过功率限制、擅自更换天线或其他规避无线电法规的实现与操作说明。

## 工作流

1. 先阅读[当前状态](docs/status/current.md)、[文档入口](docs/README.md)和对应专题。
2. 普通变更从主题分支向 `dev` 发起 PR；`master` 只接收阶段性 `dev` 晋级或明确的 `hotfix/*`。
3. 主题分支使用 `feature/*`、`fix/*`、`docs/*`、`research/*`、`experiment/*`、`chore/*` 或 `hotfix/*`。
4. 提交遵循 Conventional Commits，例如 `docs(governance): define repository rules`。
5. 不在提交信息中添加 AI 协作者署名；提交作者应是对变更负责的真实贡献者。
6. PR 只记录实际执行过的验证，并显式列出未执行、受环境阻塞和需要人工或实验室完成的项目。

完整分支、合并和回流规则见 [ADR 0003](docs/adr/0003-branch-pr-and-ruleset-governance.md)。

## 本地验证

macOS、Linux 或 Git Bash：

```bash
./scripts/check-repo.sh
git diff --check
```

Windows PowerShell：

```powershell
pwsh ./scripts/check-repo.ps1
git diff --check
```

实现栈建立后，还需要执行改动范围对应的格式化、静态分析、测试和集成验证。台架、模拟或静态检查结果不能自动外推为外场、量产、法规或端到端安全结论。

## 变更说明要求

PR 应覆盖以下适用内容：

- 目标、范围和明确非目标；
- 射频地区、SKU、频段、带宽、功率和天线影响；
- 身份、密钥、端到端加密、升级和用户数据影响；
- 公共协议、兼容性、迁移与失败模式；
- 证据属于产品目标、架构候选、厂商声明、第三方结果还是 RadishLink 实测；
- 实际验证、未验证内容、风险和回滚方式；
- 目标为 `master` 时的 `master -> dev` 回流负责人和方式。

## 审查原则

- 结论不得超过证据覆盖范围。
- 中继默认只处理密文与最少路由元数据。
- 手机接入不能成为设备核心通信的前置条件。
- 应用协议不能绑定单一厂商或单一 L2 Mesh 才能成立。
- 尚未通过三节点、端到端安全和对应环境指标验证的能力，继续标记为“目标”“候选”或“待验证”。
