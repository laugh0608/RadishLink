# RadishLink 协作约定

本文件为人工与 AI 协作者提供稳定规则，只约束 RadishLink。

## 称呼与语言

- 对话开始或结束总结时称呼用户为 `萝卜SAMA`。
- 默认使用中文讨论和编写项目文档。
- 命令、路径、协议字段、芯片型号、标准名和外部项目名保留原文。

## 开始任务前

1. 运行 `git status --short --branch`；若实际分支、ahead / behind、暂存区或工作树与用户描述不一致，先报告差异，不用 reset、checkout 或清理命令制造预期状态。
2. 区分本次任务改动、用户已有改动和可能的并行改动，只把任务范围内文件纳入修改、验证、暂存和提交。
3. 先读 `docs/status/current.md` 和 `docs/README.md`，再按任务读取对应专题，不默认展开无关历史和研究材料。
4. 明确任务属于说明、诊断、设计、实现、治理、发布还是监控，不把一种授权扩大成另一种操作。
5. 区分产品目标、架构候选、已接受 ADR、厂商声明、第三方结果和 RadishLink 实测。
6. 识别任务是否影响射频法规、安全边界、设备身份、密钥、升级、公共协议、持久化格式、许可证、依赖或用户数据。
7. 先查找已有实现、测试、脚本、命名和目录惯例；没有充分理由时不另造平行入口、重复 helper 或第二套配置口径。
8. 涉及分支、PR、CI、Ruleset 或远程设置时，先读 `docs/governance/repository-governance.md` 和 ADR 0003。

## 协作流程与授权范围

- 用户明确要求修改且范围清楚、风险可控时，直接实施并完成风险匹配的验证。
- 用户只要求分析、解释、诊断、审查或建议时，不擅自修改文件、提交或 GitHub 远程状态；诊断授权不自动包含修复。
- 需求不明确，或缺失选择会实质改变架构、安全、兼容性、许可证、用户数据或外部状态时，先说明关键判断并请求确认。
- 授权只覆盖当前任务中已经说明的动作、目标和影响；历史会话授权、笼统长期授权或相邻动作授权不自动沿用。
- 优先解决根因和边界问题，不用层层 fallback、默认成功、静默吞错或隐藏失败制造表面稳定。
- 控制修改范围，但“范围小”不能成为交付不完整、证据不足或留下明显不一致的理由。
- 不混入无关重构、格式化、依赖升级或目录搬迁；相邻问题只有在阻断当前目标或用户明确扩展范围时才处理。
- 完成可分割步骤后执行对应最小验证；修改长期规则时同步检查文档、自动化和远程配置模板是否一致。

## 方案与变更分级

- 错字、链接、格式等不改变事实与流程的 L0 变更，可以直接实施并做精准验证。
- 仓库脚本或局部实现等 L1 变更，需要对应格式化、静态检查、精准测试和仓库检查。
- 架构、公共协议、持久化格式、身份、密码、升级、用户数据等 L2 变更，先确认专题或 ADR 边界，说明兼容性、迁移、失败模式和正负例。
- 射频发射、固件刷写、依赖安装、系统配置、远程 Ruleset、发布、签名或真实设备/用户数据等 L3 外部状态变更，执行前必须说明命令、目标、副作用和回滚并获得明确授权。
- 风险等级决定验证深度，不自动扩大用户授权；历史会话中的授权不能沿用到新的外部状态变更。

## 工作区与变更范围

- 已暂存、未暂存、未跟踪和其他协作者写入的内容默认属于用户，不覆盖、不回滚、不顺手提交。
- 修改与现有差异重叠时先理解用户改动并做最小兼容编辑；无法安全合并时停止写入并说明冲突，不以重写整文件规避判断。
- 破坏性操作前解析并复核精确目标；不得对仓库根、用户目录、未解析变量或宽泛 glob 执行递归删除、覆盖或批量移动。
- 临时脚本、日志、截图、测试数据和调试产物放入任务专用且已忽略的位置；只清理本任务创建且确认无保留价值的内容。
- 不为通过格式门禁批量改写第三方材料、上游镜像、生成文件或明确需要保留原格式的资料。
- 未经明确授权不跨工作区修改兄弟项目、参考仓库或其他本地项目；读取参考资料不自动授权写入。
- 修改后复核 `git diff`、`git status` 和未跟踪文件，确认没有越界文件、秘密、缓存、意外换行、权限变化或测试产物。

## 当前项目边界

- 当前阶段是定义、法规预检和三节点 POC 准备，不直接设计量产 PCB。
- 主系统基线是嵌入式 Linux；ESP32-S3 是可选辅助 MCU，不是视频与完整 Mesh 主控。
- Wi-Fi HaLow / MM8108 是候选，不是已冻结的量产与全球可用方案。
- 802.11s 可以作为原型承载层，但应用协议不得依赖单一厂商或单一 L2 Mesh 才能成立。
- 每个正式节点都应具备终端与中继能力；是否实际中继由用户策略和资源状态决定。
- 手机是附近的伴侣终端，设备在没有手机时仍必须完成核心通信。
- WPA2/WPA3、TLS 或 QUIC 等逐跳保护不能替代消息和媒体的端到端加密。

## 无线电与安全停止线

- 在 `docs/regulatory/radio-compliance.md` 的目标地区、SKU、频段、带宽、功率和天线结论完成前，不执行外场 HaLow 发射测试。
- 不运行解锁区域、绕过监管域、提高功率、加装功放或擅自更换天线的命令。
- 射频测试必须记录监管域、固件、信道、带宽、功率、天线、距离、视距条件和高度。
- 不自研密码算法；密码协议和库必须有清楚的标准、维护状态、许可证和安全评审路径。
- 真实密钥、恢复码、设备凭据和私密通信内容不得进入日志、fixture、截图、提交或测试文档。
- 中继只处理密文和最少路由元数据；调试便利不能成为解密内容的理由。

## 文档真相源

- `LICENSE`：仓库原创内容的许可真相源；第三方材料遵循各自许可证。
- `SECURITY.md`：漏洞报告、安全问题范围和协调披露方式。
- `CONTRIBUTING.md` / `CODE_OF_CONDUCT.md`：外部贡献入口和社区行为边界。
- `docs/status/current.md`：当前阶段、风险和下一步。
- `docs/product-definition.md`：长期定位、能力层级与产品边界。
- `docs/architecture/`：系统、网络与路由边界。
- `docs/security/`：威胁模型、身份、加密和升级安全。
- `docs/hardware/`：原型与量产硬件策略，不承载法规结论。
- `docs/regulatory/`：频率与设备合规前置条件。
- `docs/testing/`：可复现测试方法与通过条件。
- `docs/research/`：外部证据和证据限制，不自动成为正式决策。
- `docs/adr/`：已接受、替代或废弃的长期架构决策。

文档规则：

- 文档、实现、自动化和远程状态冲突时，先判断哪一方过期，再统一修正，不通过放宽检查掩盖漂移。
- 优先更新已有正式文档；入口只保留当前结论、停止线和索引，历史推演、验证流水和长背景放入专题或记录。
- `docs/research/`、测试记录、周志和归档只保存证据，不自动成为正式产品、架构或治理决策。
- 新增或大改长文档时说明用途、目标读者和明确非目标；不要让同一文件同时承担入口、规范、历史流水和完整参考。
- 引用兄弟项目时使用项目名和稳定在线地址；任务临时提供的本机绝对路径不得进入长期文档。
- 项目日期使用 Asia/Shanghai（UTC+8）。

带数字的声明遵循以下规则：

- “支持最高 43.33 Mbps”属于 PHY 峰值，不得写成应用吞吐；
- “目标 1 km”必须同时说明视距、单跳、合法功率和验收指标；
- 未经 RadishLink 自有测试的数据标为厂商声明、标准目标或第三方结果；
- 国内外频段规则分别确认，不能用芯片可调频范围推导合法可用范围。

## 文件与代码规范

- 使用最小但完整的改动解决当前目标，不复制兄弟项目的产品边界。
- `AGENTS.md` 与 `CLAUDE.md` 必须逐字一致。
- 仓库自有文本使用 UTF-8 无 BOM、LF、末尾换行且无尾随空格。
- 换行、缩进、二进制类型和忽略规则遵循 `.editorconfig`、`.gitattributes` 与 `.gitignore`。
- 命名必须表达领域职责，不新增含义不清的缩写、`Manager`、`Helper`、`Context`、`Adapter` 或只包装技术动作的抽象。
- 错误必须保留上下文和真实原因，不静默吞掉失败；控制流优先早返回、显式分支和小而清晰的函数，不写深层嵌套或只转发参数的 wrapper。
- 抽象只在减少真实重复、隔离稳定边界、降低复杂度或表达明确领域概念时引入，不为未来可能需要预建空框架。
- 公共协议、持久化格式或外部接口必须有版本或明确演进策略，并覆盖兼容、迁移、未知字段和拒绝路径。
- 单个源码文件接近 1000 行时评估按职责拆分，原则上不继续超过 1500 行；生成文件和有来源的上游材料除外。
- committed 相对路径默认不超过 180 字符，单文件默认不超过 10 MiB；目录按浅层职责分组，不用深层树编码长自然语言语义。
- 新增或修改 Markdown 后检查相对链接；新增 JSON 后验证语法。

## 依赖、第三方材料与生成产物

- 标准库或现有依赖能够清楚解决问题时，不为便利新增依赖；新增时说明用途、版本、维护状态、许可证、供应链风险和可替代方案。
- 依赖声明与 lockfile 必须通过对应包管理器维护，不手工伪造解析结果；意外 lockfile 漂移必须查明原因。
- 引入第三方代码、固件、数据、模型、字体、图标、示例或资产时记录来源、版本、许可证和必要归属；公开可访问不等于可以自由复制或再分发。
- 生成代码、绑定、schema、fixture、固件清单或测试向量必须能追溯生成入口和输入；需要提交时说明再生成方式和人工修改边界。
- 测试、文档和示例使用合成或脱敏数据，不复制真实用户通信、设备凭据、安全报告、专有输入或生产秘密。

## 验证策略

- 文档改动至少运行 `./scripts/check-repo.sh` 和 `git diff --check`。
- Windows 对等入口为 `pwsh ./scripts/check-repo.ps1`；只需在 Windows 变更或跨平台验收时实际执行。
- 实现改动还需执行对应格式化、静态检查、测试和必要的三节点集成验证。
- 修复缺陷时优先先加入能暴露原问题的回归用例，再修正实现；无法自动复现时记录人工复现条件和证据限制。
- L2 变更必须覆盖正例、负例、兼容性、迁移、降级、资源上限和失败恢复中适用的部分。
- 测试通过只证明测试覆盖的条件，不把台架结果外推为外场、量产或法规结论。
- 静态检查、mock、headless、仿真或两节点结果不能替代其未覆盖的真实入口、三节点、多跳、平台行为或人工可见结果。
- 命令成功退出不自动代表目标达成；仍需检查预期产物、状态变化、诊断、负向结果和残留进程。
- 验证失败先保存命令、退出码和关键错误，再判断代码、数据、测试、工具或环境根因；不得只因重跑成功就忽略不稳定性。
- 后续若提供 quick / fast 模式，它只用于开发反馈，不自动替代阶段合并、发布或高风险变更的正式门禁。
- PR、交接和测试文档只记录实际运行的验证；未执行、失败、受环境阻塞和需要人工/实验室完成的项目分别说明。
- 分支、required context、Ruleset、workflow 或合并策略变化时，同步更新 ADR、治理文档、Ruleset 模板与说明、PR 模板、检查器和协作文件。

## 自动化与外部环境

- 会改变外部状态、依赖、进程、系统或用户数据的动作前，说明精确命令或操作、目标、主要副作用、预计持续时间、清理或回滚方式，并取得当前任务的明确授权。
- 启动长期服务、桌面应用、浏览器、虚拟机或人工交互进程前单独确认；AI 启动的进程在任务结束前按约定关闭并报告，用户自行启动的进程不擅自停止。
- 沙盒、权限、PATH、网络、证书或平台差异导致关键验证失败时，保留原始错误并识别环境特征；必要时申请在更真实环境复验同一命令，不为适应异常环境修改产品代码。
- 提权只用于已说明的验证或用户明确授权的外部动作，不借提权扩大文件、依赖、系统或远程修改范围。
- token、私钥、恢复码、设备凭据和真实敏感输入不得出现在命令参数、日志、截图、fixture、提交或对话摘录中。

## Git 与外部状态

- 未经明确要求不提交、push、创建远程仓库、PR、tag、Release 或部署。
- commit 授权不包含 push、PR、tag、Release、部署或远程设置写入；这些动作必须分别确认。
- 未经明确要求不 amend、重写、合并或拆分既有提交，不修改用户 Git 身份，不使用 `reset --hard`、rebase、强制推送或其他破坏性操作处理共享历史。
- 提交使用 Conventional Commits，不添加 AI 协作者署名。
- 提交按可审阅主题拆分；暂存前复核完整 diff，工作区存在无关改动时按精确路径暂存。
- 小型提交使用清楚的单行主题；大型提交在正文用 3–6 条简短说明记录主要变化和重要边界。
- 提交后复核提交摘要与工作区状态，报告 commit hash、剩余改动以及相对远端的 ahead / behind 状态。
- `master` 是稳定主线，只通过 PR 接收阶段性 `dev` 晋级或 `hotfix/*`；`dev` 是常态开发与集成分支。
- 主题分支使用 `feature/*`、`fix/*`、`docs/*`、`research/*`、`experiment/*`、`chore/*` 或 `hotfix/*`，普通 PR 默认面向 `dev`。
- `dev -> master` 优先 merge commit，允许 rebase merge，禁用 squash merge；任何 `master` 合并后都必须在下一轮开发前回流 `dev`。
- 共享 `dev` 不通过 rebase、reset 或 force push 伪造同步；回流后验证 `origin/master` 是 `dev` 的祖先。
- `master` 远程保护只绑定 strict 的 `Candidate Quality`；当前单人阶段不保护 `dev`，不要求装饰性 CODEOWNERS 或自我审批。
- 仓库 Ruleset JSON、workflow 和文档只是声明；只有远程实际创建并通过测试 PR 复核后才能写成已启用。
- 依赖安装、系统配置、射频发射、固件刷写、GitHub 设置和外部设备状态变更在执行前说明目标与副作用。

## CI 与供应链

- `Candidate Quality` 是稳定聚合 context；新增检查作为独立组件接入，不随技术栈演进频繁更名远程 required context。
- GitHub Actions 使用最小权限、`pull_request` 触发器和完整 commit SHA 固定外部 Action；不在特权触发器中执行不可信 PR 代码。
- 公共或不可信 PR 不使用持有签名密钥、设备凭据、内网访问或真实硬件控制权的 self-hosted runner。
- 技术栈冻结前不创建占位构建绿灯；版本、兼容性、签名和发布载体冻结前不创建自动发布或 tag Ruleset。
- Action、依赖、固件工具链和外部材料必须有清楚来源、版本、许可证与更新审查路径。

## 完成与交接

- 先说明实际结果，再列关键文件、验证和仍未关闭的风险。
- 明确区分已完成、未执行、受环境阻塞和需要人工/实验室处理的项目。
- 报告假设、兼容性影响、失败模式和结论仍不覆盖的环境。
- 报告工作区是否干净，是否存在未提交或未推送提交、后台进程或任务专用临时产物。
- 报告远程分支、Ruleset、PR、Release 等外部状态是否实际改变；未执行的远程动作不能由本地模板代替。
- 只有任务范围内必要工作和风险匹配验证已经完成时才表述为“完成”；占位实现、待授权动作和关键验证阻塞必须显式保留。
- 只有三节点、端到端安全和对应环境指标真实通过后，才把能力从“目标”改成“已验证”。

## 通用开发原则

1. 交付小而完整的真实纵向切片，不以占位、演示路径或玩具式最小实现冒充目标完成。
2. 验证按风险分层；公共边界、核心流程、数据一致性、安全、隐私和用户可见行为扩大验证。
3. 代码优先清晰、直观和可维护；设计模式、抽象或元编程只有在明显降低复杂度时才引入。
4. 修改前理解现有模块、数据和调用边界，沿用稳定结构；不做无关重构，但修正直接阻断当前目标的局部结构问题。
5. 不用异常吞噬、默认值、重试或多层 fallback 制造表面安全；所有兜底必须有明确触发条件、可观测性和停止线。
6. 优先显式类型、结构化数据、标准能力和顺序控制流，避免动态字符串状态、全局隐式状态和不必要嵌套。
7. 在需求完整和质量成立的前提下控制修改范围；每个新增文件、依赖、抽象和兼容层必须有当前用途。
8. 结论不能超过证据能力；静态检查、测试、仿真、台架结果和外场实测必须使用各自准确术语。

## 判断顺序

1. 需求和声明是否完整、准确。
2. 法规、安全、隐私、许可证和证据边界是否清晰。
3. 架构、协议、数据和兼容性是否可长期维护。
4. 修改范围与实现复杂度是否受控。
5. 验证是否与风险匹配，且结论没有超过证据能力。
