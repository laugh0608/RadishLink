# RadishLink 协作约定

本文件为人工与 AI 协作者提供启动级长期约束，只约束 RadishLink。

## 定位与优先级

- `docs/` 是产品、架构、规则和决策的正式真相源；本文件只保留跨任务、跨阶段且必须启动即生效的约束，不承载当前阶段、临时门禁、历史记录或专题实现细节。
- 当前阶段、优先级、风险、停止线和“当前不做”以 `docs/status/current.md` 为准；产品、架构、安全、法规、验证和治理细节以对应专题与 Accepted ADR 为准。
- 在适用法律、许可证和安全停止线内，用户在当前任务中的明确要求优先于仓库默认流程；若要求会改变架构、安全、兼容性、许可证、用户数据或外部状态，仍须先说明影响并确认范围。
- 只读取当前任务需要的文档；研究、测试记录和归档只在需要追溯证据时按需读取。

## 称呼与语言

- 对话开始或结束总结时称呼用户为 `萝卜SAMA`。
- 默认使用中文讨论和编写项目文档。
- 命令、路径、协议字段、芯片型号、标准名和外部项目名保留原文。

## 开始任务

1. 运行 `git status --short --branch`；若分支、ahead / behind、暂存区或工作树与用户描述不一致，先报告差异，不用 reset、checkout 或清理命令制造预期状态。
2. 先读 `docs/status/current.md` 和 `docs/README.md`，再按下表读取任务所需专题；不默认展开无关历史和研究材料。
3. 明确任务属于说明、诊断、设计、实现、治理、发布还是监控，不把一种授权扩大成另一种操作。
4. 识别任务是否影响射频法规、安全边界、设备身份、密钥、升级、公共协议、持久化格式、许可证、依赖、用户数据或外部状态。
5. 先查找已有实现、测试、脚本、命名和目录惯例；没有充分理由时不另造平行入口、重复 helper 或第二套配置口径。

## 协作与授权

- 用户明确要求修改且范围清楚、风险可控时，直接实施并完成风险匹配的验证。
- 用户只要求分析、解释、诊断、审查或建议时，不擅自修改文件、提交或改变远程状态；诊断授权不自动包含修复。
- 需求不明确，或缺失选择会实质改变架构、安全、兼容性、许可证、用户数据或外部状态时，先说明关键判断并请求确认。
- 授权只覆盖当前任务中已说明的动作、目标和影响；历史会话、笼统长期授权或相邻动作授权不自动沿用。
- 控制修改范围但保证交付完整；不混入无关重构、格式化、依赖升级或目录搬迁，不用层层 fallback、默认成功、静默吞错或隐藏失败制造表面稳定。
- 详细任务推进、工作区保护、实现、验证与交接规则见 `docs/governance/agent-collaboration.md`。

## 高风险与停止线

- 变更风险按 `docs/governance/repository-governance.md` 的 L0–L3 分级；等级决定审查和验证深度，不扩大用户授权。
- 射频发射、固件刷写、依赖安装或更新、系统配置、长期服务、桌面应用、浏览器或虚拟机启动、GitHub 设置、发布、签名以及真实设备或用户数据操作属于 L3；执行前必须说明精确目标、命令或操作、主要副作用、预计持续时间和清理或回滚方式，并取得当前任务的明确授权。
- 执行无线相关操作前必须读取当前状态和 `docs/regulatory/radio-compliance.md`；不解锁区域、不绕过监管域、不提高功率、不加装功放，也不擅自更换天线。
- 不自研密码算法；逐跳链路保护不能替代消息和媒体的端到端加密，中继不得因调试便利获得内容明文。
- token、私钥、恢复码、设备凭据、私密通信和真实敏感输入不得进入命令参数、日志、fixture、截图、提交或测试文档。
- 破坏性操作前解析并复核精确目标；不得对仓库根、用户目录、未解析变量或宽泛 glob 执行递归删除、覆盖或批量移动。
- 提权只用于已说明的验证或已获授权的外部操作，不借提权扩大文件、依赖、系统或远程修改范围。

## 任务文档路由

| 任务 | 优先读取 |
| --- | --- |
| 当前阶段、优先级、停止线与下一步 | `docs/status/current.md`，必要时进入 `docs/status/` 对应计划 |
| 长期定位、能力层级与产品边界 | `docs/product-definition.md`、`docs/roadmap.md` |
| 系统、网络、路由与媒体协议 | `docs/architecture/`、`docs/protocol/`、相关 ADR |
| 身份、加密、升级与敏感数据 | `docs/security/`、`SECURITY.md`、相关 ADR |
| 硬件候选、采购与原型边界 | `docs/hardware/`、当前状态 |
| 频段、地区、功率、天线与射频测试 | `docs/regulatory/`、`docs/testing/`、当前状态 |
| 验证方法、通过条件与证据限制 | `docs/testing/`、`docs/research/`、当前状态 |
| 分支、PR、CI、Ruleset 与风险分级 | `docs/governance/repository-governance.md`、ADR 0003 |
| Agent 详细协作、验证与交接 | `docs/governance/agent-collaboration.md` |

## 工作区与实现原则

- 已暂存、未暂存、未跟踪和其他协作者写入的内容默认属于用户；不覆盖、不回滚、不顺手提交，重叠时先理解并做最小兼容编辑。
- 未经明确授权不跨工作区修改兄弟项目、参考仓库或其他本地项目；读取参考资料不自动授权写入。
- 优先解决根因和边界问题，沿用稳定结构；抽象只在减少真实重复、隔离稳定边界、降低复杂度或表达明确领域概念时引入。
- 错误必须保留上下文和真实原因；公共协议、持久化格式和外部接口必须有版本或明确演进策略，并覆盖兼容、迁移、未知字段和拒绝路径。
- 标准库或现有依赖能够清楚解决问题时不新增依赖；第三方代码、固件、数据、模型和资产必须记录来源、版本、许可证、归属和更新路径。
- 仓库自有文本遵循 `.editorconfig`、`.gitattributes` 与 `.gitignore`，使用规定的编码、换行和文件末尾换行；测试和文档只使用合成或脱敏数据。

## 证据、验证与交接

- 区分产品目标、架构候选、Accepted ADR、厂商声明、第三方结果、台架结果和 RadishLink 实测；结论不得超过证据覆盖的环境与条件。
- 距离、速率、时延、功耗等数字必须标明条件或目标属性；PHY 峰值不得写成应用吞吐，芯片可调频范围不得推导为地区合法频段。
- 只有相应三节点、端到端安全和环境指标真实通过后，才把能力从“目标”改成“已验证”。
- 文档改动至少运行 `./scripts/check-repo.sh` 和 `git diff --check`；实现改动按风险执行格式化、静态检查、精准测试和必要的集成验证。
- 验证通过只证明已覆盖条件；失败时保留命令、退出码和关键错误，不因重跑成功忽略不稳定性，也不以 mock、仿真或两节点结果替代未覆盖的真实入口。
- 交接先说明实际结果，再列关键文件、已执行验证、未执行或受阻项目、剩余风险、工作区与后台进程状态，以及远程状态是否真实改变。

## Git 与外部状态

- `dev` 是常态开发与集成分支；串行推进的普通任务直接在 `dev` 开发和提交，不要求主题分支、Pull Request 或额外 worktree。
- 只有项目所有者明确要求、外部贡献、并行写入、确有隔离价值的高风险改动或 hotfix 才创建主题分支；Agent 不自动创建 `codex/*` 等临时分支。
- `dev` 当前不启用 branch protection，普通 push 不触发 CI；直接开发按改动范围完成本地验证，需要评审或隔离时再通过 Pull Request 合入 `dev`。
- 未经明确要求不提交、push、创建或修改远程仓库、PR、tag、Release、Ruleset 或部署；commit 授权不包含任何远程写入。
- 未经明确要求不 amend、重写、合并或拆分既有提交，不修改用户 Git 身份，不用 rebase、reset 或 force push 改写共享历史。
- 提交使用 Conventional Commits，按可审阅主题拆分，不添加 AI 协作者署名；分支、合并和回流细节以仓库治理文档与 ADR 0003 为准。

## 入口文件维护

- `AGENTS.md` 与 `CLAUDE.md` 只在跨任务、跨阶段且必须启动即生效的长期约束发生变化时修改。
- 阶段状态、临时门禁、“当前不做”、批次事实和专题细节应更新对应 `docs/` 真相源，不得复制回根入口；`docs/README.md` 只提供短说明和索引。
- 一条规则只有同时满足“跨任务、跨阶段、必须在读取专题前生效、无法仅靠文档路由可靠承载”时，才应进入根入口。
- 两个文件必须逐字一致；修改任一文件时同步另一份并执行 `./scripts/check-repo.sh`。
